{{> header}}
<body>
    <h1>Skate Park</h1>

    <div class="py-4">
      <h2>Administración</h2>
      <hr class="w-50 mx-auto" />

      <table class="table w-50 m-auto">
        <thead>
          <tr>
            <th scope="col">#</th>
            <th scope="col">Foto</th>
            <th scope="col">Nombre</th>
            <th scope="col">Años de experiencia</th>
            <th scope="col">Especialidad</th>
            <th scope="col">Estado</th>
          </tr>
        </thead>
        <tbody>

          {{#each participantes as |participante|}}
             <tr>
             {{> filaParticipante participante=participante}}
             {{#if participante.estado}}
                <td><input class="checkbox-admin" type="checkbox" data-id="{{participante.id}}" checked /></td>
              {{else}}
                <td><input class="checkbox-admin" type="checkbox" data-id="{{participante.id}}" /></td>
             {{/if}}
             </tr>
          {{/each}}

          {{!-- <tr>
            <th scope="row">2</th>
            <td><div></div></td>
            <td>Evelien Bouilliart</td>
            <td>10</td>
            <td>Heelflip</td>
            <td><input type="checkbox" checked /></td>
          </tr>
          <tr>
            <th scope="row">3</th>
            <td><div></div></td>
            <td>Danny Way</td>
            <td>8</td>
            <td>Ollie</td>
            <td><input type="checkbox" /></td>
          </tr> --}}

        </tbody>
      </table>
    </div>
{{> footer}}